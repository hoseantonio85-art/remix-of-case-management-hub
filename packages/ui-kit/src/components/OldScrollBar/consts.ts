export enum EScrollOffsetAttributes {
  x = 'scrollLeft',
  y = 'scrollTop',
}

export enum EScrollSizeAttributes {
  x = 'scrollWidth',
  y = 'scrollHeight',
}

export enum EOffsetSizeAttributes {
  x = 'offsetWidth',
  y = 'offsetHeight',
}

export enum EOffsetAttributes {
  x = 'left',
  y = 'top',
}

export enum ESizeAttributes {
  x = 'width',
  y = 'height',
}
