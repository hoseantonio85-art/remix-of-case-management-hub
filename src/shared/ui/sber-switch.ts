/**
 * После установки @sber-orm/ui-kit в этой точке должен быть только:
 *
 *   export * from "@sber-orm/ui-kit";
 *
 * Тогда `src/shared/ui/index.tsx` сможет переключиться на пакет,
 * не затрагивая ни одного продуктового компонента (все они импортируют
 * только из "@/shared/ui").
 *
 * Сейчас пакет недоступен (404 в реестре), поэтому переключатель
 * закомментирован и фактически экспортируется локальный adapter-layer.
 */

// export * from "@sber-orm/ui-kit";
export {};